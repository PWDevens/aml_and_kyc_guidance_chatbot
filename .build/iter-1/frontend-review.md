No UI in this iteration; skipped. `changes.md` confirms `src/app/static/**` was not
modified (SSE event names/shape unchanged, so the existing UI needs no changes).
Cognitus UI/UX work is scoped to Iteration 4.
