No UI in this iteration; skipped. `changes.md` confirms no `src/app/static/**` changes —
`/corpus_status` gained two new JSON keys (`last_etl_run`, `counts_by_source`) that the
existing UI simply ignores. Cognitus UI/UX work is scoped to Iteration 4.
