No UI in this iteration; skipped. `changes.md` confirms `src/app/static/**` was not
modified — this iteration only adds a new `verification` SSE event, which the
existing UI simply ignores (no rendering added). Rendering the verification/decline
state and the FAQ "from cache" marker is Iteration 4's job (Cognitus UI/UX).
